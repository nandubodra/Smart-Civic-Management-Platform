'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
import { ArrowUpRight, Clock3, LogOut, MapPin, Image as ImageIcon } from 'lucide-react'
import { supabase } from '@/lib/supabase'

type Complaint = { id:string; waste_type:string; status:string; created_at:string; location_lat:number|null; location_lng:number|null; photo_url:string|null; resolution_photo_url:string|null; resolved_at:string|null; resolution_notes:string|null }
export default function AccountPage() { const [rows,setRows]=useState<Complaint[]>([]); const [loading,setLoading]=useState(true); useEffect(()=>{let active=true; const load=async()=>{if(!supabase){setLoading(false);return}; const {data:{user}}=await supabase.auth.getUser(); if(!user)return; const {data}=await supabase.from('complaints').select('id,waste_type,status,created_at,location_lat,location_lng,photo_url,resolution_photo_url,resolved_at,resolution_notes').eq('user_id',user.id).order('created_at',{ascending:false}); if(active){setRows(data||[]);setLoading(false)}};load();return()=>{active=false}},[]); const signOut=async()=>{await supabase?.auth.signOut();window.location.href='/'}; return <div className="mx-auto max-w-5xl px-6 py-14 lg:px-10"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.25em] text-gold">Citizen account</p><h1 className="mt-4 text-5xl md:text-6xl">Your civic journey.</h1><p className="mt-4 text-navy/55">Track every report and see the evidence behind each resolution.</p></div><button onClick={signOut} className="flex items-center gap-2 self-start rounded-full border border-gold/40 px-5 py-3 text-xs font-semibold uppercase tracking-widest"><LogOut size={15}/>Sign out</button></div><div className="mt-12 flex items-center justify-between"><h2 className="text-3xl">Your reports</h2><Link href="/report" className="flex items-center gap-2 text-sm font-semibold text-gold">New report <ArrowUpRight size={16}/></Link></div>{loading?<div className="mt-6 grid gap-3">{[1,2,3].map(x=><div key={x} className="h-24 animate-pulse rounded-2xl bg-navy/5"/>)}</div>:rows.length===0?<div className="glass mt-6 rounded-2xl p-10 text-center shadow-luxury"><p className="serif text-3xl">No reports yet.</p><p className="mt-3 text-sm text-navy/55">Your first report can help make a visible difference.</p><Link href="/report" className="mt-6 inline-block rounded-full bg-navy px-6 py-3 text-sm text-ivory">Report waste</Link></div>:<div className="mt-6 space-y-5">{rows.map(row=><div key={row.id} className="glass rounded-2xl p-5 shadow-luxury"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="text-sm font-semibold">{row.waste_type}</p><p className="mt-2 flex items-center gap-2 text-xs text-navy/50"><Clock3 size={13}/>{new Date(row.created_at).toLocaleDateString()} {row.location_lat&&<><MapPin size={13}/>{Number(row.location_lat).toFixed(3)}, {Number(row.location_lng).toFixed(3)}</>}</p></div><div className="flex items-center gap-4"><span className="rounded-full bg-mist px-3 py-1 text-xs capitalize">{row.status.replace('_',' ')}</span><span className="font-mono text-xs text-gold">#{row.id.slice(0,8)}</span></div></div>{row.resolution_photo_url&&<div className="mt-5 border-t border-gold/15 pt-5"><div className="mb-3 flex items-center gap-2 text-xs font-semibold uppercase tracking-widest text-green-700"><ImageIcon size={15}/>Before & after proof · {row.resolved_at&&new Date(row.resolved_at).toLocaleString()}</div><div className="grid gap-3 sm:grid-cols-2"><div><p className="mb-2 text-xs text-navy/50">Before</p>{row.photo_url?<img src={row.photo_url} alt="Original issue evidence" className="h-48 w-full rounded-xl object-cover"/>:<div className="grid h-48 place-items-center rounded-xl bg-navy/5 text-xs text-navy/40">No original photo</div>}</div><div><p className="mb-2 text-xs text-navy/50">After</p><img src={row.resolution_photo_url} alt="Resolution evidence" className="h-48 w-full rounded-xl object-cover"/></div></div>{row.resolution_notes&&<p className="mt-3 text-sm text-navy/55">{row.resolution_notes}</p>}</div>}</div>)}</div>}</div> }
